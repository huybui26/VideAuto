node_modules/
dist/
output/
coverage/
.env.local
.env
*.log
.DS_Store

# Big SFX library (downloaded by `npm run sfx:download` — too large to commit)
SFX/

# Background music library — media files, same policy as the SFX library
assets/bgm/
